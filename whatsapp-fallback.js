/* Compatibility stub.
   The live Jazz GitHub Pages deployment loads the maintained WhatsApp fallback
   module from whatsapp-fallback/jazz-fallback.js via .github/workflows/pages.yml.
   This file intentionally does not create a second fallback UI or heartbeat.
*/
console.info('Jazz WhatsApp fallback is loaded by the maintained module.');
